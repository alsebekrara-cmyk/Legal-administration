# 📥 دليل الاستيراد الكامل

## 🎯 الخطوات الأساسية

### 1️⃣ افتح التطبيق
- افتح `index.html` في المتصفح
- أو شغل التطبيق عبر Electron

### 2️⃣ استورد البيانات
1. اذهب إلى صفحة **الإعدادات** من القائمة الجانبية
2. اضغط على زر **استعادة من نسخة احتياطية**
3. اختر ملف `sample-data.json`
4. انتظر رسالة النجاح وإعادة تحميل الصفحة

### 3️⃣ تحقق من البيانات
- افتح **لوحة التحكم** - يجب أن ترى الإحصائيات
- افتح **الدعاوى** - يجب أن ترى قائمة الدعاوى
- افتح **المحامين** - يجب أن ترى قائمة المحامين
- افتح **المدعى عليهم** - يجب أن ترى قائمتهم

---

## 🛠️ أدوات التشخيص

افتح **Console** في المتصفح (F12) واستخدم هذه الأوامر:

### فحص البيانات الحالية
```javascript
debugData()
```
يعرض لك:
- عدد الدعاوى
- عدد المحامين
- عدد المدعى عليهم
- عدد الاستقطاعات
- عينة من البيانات

### استيراد سريع
```javascript
quickImport()
```
يستورد `sample-data.json` مباشرة

### تحديث الجداول
```javascript
refreshAllTables()
```
يُحدث جميع الجداول دون إعادة تحميل الصفحة

### مسح البيانات
```javascript
clearAllData()
```
⚠️ يحذف جميع البيانات (يطلب تأكيد)

---

## 🔍 استكشاف الأخطاء

### المشكلة: البيانات لا تظهر بعد الاستيراد

#### الحل 1: تحقق من Console
```javascript
debugData()
```
إذا ظهر `cases: 0` معناها الاستيراد لم ينجح

#### الحل 2: افحص localStorage
```javascript
localStorage.getItem('legalAppData')
```
إذا كان `null` معناها البيانات لم تُحفظ

#### الحل 3: امسح وأعد الاستيراد
```javascript
clearAllData()
// ثم استورد الملف مرة أخرى من الإعدادات
```

#### الحل 4: استيراد مباشر
```javascript
quickImport()
```

---

## 📊 التحقق من نجاح الاستيراد

بعد الاستيراد يجب أن ترى في Console:

```
📥 بدء استيراد البيانات...
🔄 اكتشاف ملف Firebase - جاري التحويل...
🔄 بدء تحويل بيانات Firebase...
  📋 الدعاوى الخام: 100+
  ✅ تم تحويل 100+ دعوى
  👥 المدعى عليهم الخام: XX
  ✅ تم تحويل XX مدعى عليه
  👨‍⚖️ المحامين الخام: XX
  ✅ تم تحويل XX محامي
💾 حفظ البيانات في localStorage...
📊 تم الاستيراد بنجاح:
  - الدعاوى: 100+
  - المدعى عليهم: XX
  - المحامين: XX
  - الاستقطاعات: XX
🔄 إعادة تحميل الصفحة...
```

---

## 💡 نصائح مهمة

1. **استخدم Chrome أو Edge** للحصول على أفضل أداء
2. **افتح Console** (F12) لرؤية تفاصيل العمليات
3. **انتظر 1-2 ثانية** بعد رسالة النجاح
4. **لا تغلق التطبيق** أثناء الاستيراد
5. **احتفظ بنسخة احتياطية** من البيانات الأصلية

---

## 🔧 إعادة تعيين كاملة

إذا واجهت مشاكل، جرب:

```javascript
// 1. مسح كل شيء
localStorage.clear()

// 2. إعادة تحميل الصفحة
location.reload()

// 3. استيراد البيانات من جديد
// استخدم زر "استعادة من نسخة احتياطية" في الإعدادات
```

---

## 📞 دعم إضافي

إذا استمرت المشاكل:
1. افتح Console (F12)
2. اكتب `debugData()`
3. التقط صورة للنتيجة
4. شارك المشكلة مع التفاصيل
